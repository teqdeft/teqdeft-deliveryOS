# ---- Database (MySQL 8) ------------------------------------------------
# Discrete variables rather than one URL, so a password containing @ or /
# cannot silently corrupt the connection string.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=deliveryos
DB_USER=deliveryos
DB_PASSWORD=deliveryos
# Managed MySQL (PlanetScale, RDS, Aiven, most cPanel hosts) requires TLS.
DB_SSL=false
DB_POOL_MIN=2
DB_POOL_MAX=10

# Optional. Most managed hosts hand you a single URL; when set it overrides
# the discrete variables above.
# DATABASE_URL="mysql://user:password@host:3306/deliveryos"

# ---- API ---------------------------------------------------------------
PORT=4000
NODE_ENV=development
# Generate with: openssl rand -base64 48
JWT_SECRET="change-me-in-production-use-a-48-byte-random-string"
JWT_EXPIRES_IN="12h"
# Comma-separated list of origins allowed to call the API with credentials.
WEB_ORIGIN="http://localhost:5173"

# Where uploaded sources are written. Production wants S3-compatible encrypted
# storage (blueprint §15.1); the local driver is for development only.
STORAGE_DRIVER=local
STORAGE_LOCAL_PATH="./storage"

# ---- AI gateway (blueprint §8) -----------------------------------------
# Optional. With neither key set, analysis is disabled and everything else in
# the product keeps working — see §16, graceful degradation.
# A job whose configured provider has no key falls back to the other.
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""

AI_DEFAULT_PROVIDER=ANTHROPIC
# Hard ceiling on characters sent to a model in one run (§19, cost control).
# Per-job-type limits live in apps/api/src/ai/model-policy.ts.
AI_MAX_INPUT_CHARS=400000

# ---- Tests -------------------------------------------------------------
# The extraction integration tests need their own database. They skip
# themselves when MySQL is unreachable.
TEST_DB_NAME=deliveryos_test
